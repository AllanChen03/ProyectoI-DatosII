// tests.cpp
//s#include <math.h>
/*#include "imageprocessing.hpp"
#include <ext/googletest/googletest/include/gtest/gtest.h>
#include <opencv2/opencv.hpp>
#include <opencv2/opencv_modules.hpp>
#include <opencv2/photo.hpp>
#include <opencv2/core/core.hpp>
#include <opencv2/core.hpp>
#include "imageprocessing.hpp"

Mat img;
Mat *ptr_img = &img;
*ptr_img =  imread("rick.jpeg", 1);

Mat newimg(img.rows, img.cols, CV_8UC1);  
 
TEST(result, Prueba) { 
    EXPECT_EQ(0, imgs.bright_control(img, newimg, 1));
    EXPECT_EQ(0, imgs.bright_control(img, newimg, -1));
}
 
TEST(result, Prueba) {
    EXPECT_EQ(0, imgs.gamma_correction(img, newimg, img.rows, img.cols););
}

TEST(result, gamma_correction) {
    ASSERT_EQ(0, imgs.gray_scale(img, newimg, img.rows, img.cols););
   
}

TEST(result, gamma_correction) {
    ASSERT_EQ(0, imgs.gaussian_blur(img, newimg, img.rows, img.cols););
   
}
 *int main(int argc, char **argv) {
    testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}*/